namespace TsActivexGen {
    public enum TSParameterType {
        Standard,
        Optional,
        Rest
    }

    public enum FilterState {
        NotMatched,
        Match,
        DescendantMatch
    }
}
